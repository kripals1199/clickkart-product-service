# src/main/resources/application.properties
# Local bootstrap only - just enough to locate the Config Server. Everything else is pulled
# per-profile from clickkart-config-repository.
spring.application.name=clickkart-product-service
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

spring.cloud.config.uri=http://${CONFIG_SERVER_HOST:localhost}:${CONFIG_SERVER_PORT:8888}
spring.cloud.config.username=${CONFIG_SERVER_USERNAME:admin}
spring.cloud.config.password=${CONFIG_SERVER_PASSWORD:dev-only-secret-change-me}
spring.cloud.config.fail-fast=true
# The git branch (label) IS the environment. Must track SPRING_PROFILES_ACTIVE exactly.
spring.cloud.config.label=${SPRING_PROFILES_ACTIVE:dev}
spring.config.import=configserver:

management.health.probes.enabled=true
management.info.env.enabled=true

# Service-to-service surface, no Gateway route - kept out of the browser-reachable aggregated docs.
springdoc.paths-to-exclude=/internal/**

# Caps a client-supplied ?size= on catalog search so one request cannot pull the whole catalog.
spring.data.web.pageable.max-page-size=100

info.service=clickkart-product-service
